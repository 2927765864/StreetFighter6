/**
 * Scaffold: print usage. Full GLTF sampling requires three + file URLs in Node.
 * Plan Step 5 — do not invent dx at runtime; bake into move JSON after review.
 */
console.log(`estimate_move_dx.mjs scaffold
Usage (planned):
  node estimate_move_dx.mjs --glb <path> --total <n> --out <json>
See docs/plans/ai-execution-plan-anim-loco-feet-displace-v0.md Step 5.
Placeholder selfMovement for 2HK is already in app/public/data/moves/generated/ryu_2hk.json
`);
process.exit(0);
