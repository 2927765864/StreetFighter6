/**
 * Adapter over installed `three-plume@0.1.1`.
 * Import paths follow package exports (see node_modules/three-plume/package.json).
 */
export {
  Manager,
  system,
  emitter,
  RNG,
  softCircleTexture,
  type SystemDef,
  type ManagerOptions,
  type SpawnOptions,
  type System,
  type EmitterBuilder,
} from 'three-plume';
